function s = getProbeToDVAScale()
    % Get scale in `x` and `y` directions (dva)
    %
    % Returns
    % -------
    % - s: number[2]
    %   Scale [sx, sy] in dva
    
    s = getOutputField('s');
end